import os

data_dir = os.path.join('data') #Directory that contains all the intermediate (individually sorted data
data_file = os.path.join('data.txt') #File that contains the original data
final_data_file = os.path.join('final_data.txt') #File that contains the final sorted data